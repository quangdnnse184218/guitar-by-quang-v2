# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Node dependencies & builds
node_modules
dist
dist-ssr

# Environment Variables
.env
.env.local
.env.*.local
*.local

# Supabase CLI local cache (npx supabase tự tạo, không phải config của project)
supabase/.temp

# Skill bên thứ ba cài qua ui-ux-pro-max-cli (uipro init) — dữ liệu tham khảo
# cục bộ dùng để tra cứu khi code, không phải asset của project nên không commit.
.claude/skills

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
